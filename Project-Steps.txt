### 🧩 **Task Goal:**

You need to:

1. **Create a small web page** (like “Hello, I’m Vijay Ganesh”)
2. **Containerize it using Docker** (via a `Dockerfile`)
3. **Use Docker Compose** to easily run it
4. **Run everything inside your AWS EC2 Ubuntu instance**
5. **Access it in your browser** using the EC2’s public IP

---

### 🧠 **What you’re basically doing**

You’re creating a mini web app (a personal info site) that runs inside a Docker container on the cloud (EC2).
So instead of running on your local computer, it runs on a **remote virtual machine**.

---

### ⚙️ **Steps to perform this task**

#### **Step 1 — Connect to EC2**

* Launch an **Ubuntu EC2 instance** (t2.micro is fine)
* SSH into it:

  ```bash
  ssh -i your-key.pem ubuntu@your-ec2-public-ip
  ```

#### **Step 2 — Install Docker & Docker Compose**

```bash
sudo apt update
sudo apt install -y docker.io docker-compose
sudo systemctl enable docker
sudo systemctl start docker
```

---

#### **Step 3 — Create your project folder**

```bash
mkdir mywebsite
cd mywebsite
```

---

#### **Step 4 — Create a simple website**

Create an `index.html` file:

```bash
echo "<h1>Hello, I’m Vijay Ganesh</h1><p>VFX Artist & DevOps Learner</p>" > index.html
```

---

#### **Step 5 — Create a Dockerfile**

This tells Docker **how to build your container**:

```Dockerfile
# Use an official Nginx image as the base
FROM nginx:alpine

# Copy your webpage to the Nginx default directory
COPY index.html /usr/share/nginx/html/index.html
```

---

#### **Step 6 — Create a docker-compose.yml file**

This helps you **run the container easily**:

```yaml
version: '3'
services:
  web:
    build: .
    ports:
      - "80:80"
```

---

#### **Step 7 — Build and run**

```bash
sudo docker-compose up --build -d
```

---

#### **Step 8 — Test it**

* Go to your browser and open:

  ```
  http://<your-ec2-public-ip>
  ```
* You should see your basic details displayed.

---

### ✅ **What’s happening behind the scenes**

* EC2 = your remote computer in AWS
* Docker = runs isolated containers
* Dockerfile = tells how to create the image
* Docker Compose = starts your container automatically
* Browser = displays your site hosted inside Docker on EC2

---

Would you like me to show you the **exact commands you can copy-paste** into your EC2 terminal to complete the task end-to-end (no manual typing needed)?
